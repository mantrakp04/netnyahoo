{
  "name": "@netnyahoo/sync",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end-encrypted sync through a folder the user chooses (iCloud Drive, Dropbox, a NAS, a USB drive)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p .",
    "test": "swift test --scratch-path \"${TMPDIR:-/tmp}/netnyahoo-sync-swiftpm\" && node --test \"src/**/*.test.ts\""
  },
  "peerDependencies": {
    "expo": "*",
    "react-native": "*"
  },
  "devDependencies": {
    "@types/node": "^24.13.6"
  }
}
