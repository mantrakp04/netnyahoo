<section class="press" id="press" aria-labelledby="press-title" data-shot="press">
  <div class="wrap press-grid">
    <header class="press-head">
      <p class="label kicker">Press conference</p>
      <h2 class="poster" id="press-title" data-reveal>Questions from the press.</h2>
      <p class="sub" data-reveal style="--i: 1">Unlike some, we answer them.</p>
    </header>

    <dl class="qa">
      <div class="item">
        <dt>Is this a joke?</dt>
        <dd>The name is. The browser isn’t.</dd>
      </div>
      <div class="item" id="install">
        <dt>macOS won’t open it.</dt>
        <dd>
          It’s signed, not notarized yet. Open it once, click Done, then System Settings › Privacy &amp;
          Security › <b>Open Anyway</b>.
        </dd>
      </div>
      <div class="item">
        <dt>What does it collect?</dt>
        <dd>Nothing. Not even contributions.</dd>
      </div>
      <div class="item">
        <dt>Is this Dia?</dt>
        <dd>No. It just dresses like it.</dd>
      </div>
      <div class="item">
        <dt>Why “Netnyahoo”?</dt>
        <dd>A browser should leave when asked. Some offices could take notes.</dd>
      </div>
    </dl>
  </div>
</section>

<style>
  .press {
    padding-top: var(--section);
  }
  .press-grid {
    display: grid;
    grid-template-columns: repeat(12, minmax(0, 1fr));
    column-gap: var(--gutter);
    row-gap: 48px;
  }
  .press-head {
    grid-column: 1 / span 5;
    align-self: start;
    position: sticky;
    top: 100px;
  }
  .kicker {
    color: var(--ink-2);
    margin-bottom: 20px;
  }
  h2 {
    font-size: clamp(2.6rem, 1.2rem + 4vw, 5.6rem);
  }
  .sub {
    margin-top: 20px;
    font-size: var(--t-lede);
    color: var(--ink-2);
  }
  .qa {
    grid-column: 7 / -1;
    margin: 0;
    border-top: 2px solid var(--ink);
  }
  .item {
    display: grid;
    grid-template-columns: 2.4rem minmax(0, 1fr);
    padding: 24px 0 26px;
    border-bottom: 1px solid var(--rule);
  }
  dt,
  dd {
    grid-column: 2;
  }
  dt {
    font-family: var(--serif);
    font-weight: 560;
    font-size: 1.28rem;
    line-height: 1.25;
    letter-spacing: -0.01em;
    position: relative;
  }
  dt::before,
  dd::before {
    position: absolute;
    left: -2.4rem;
    font-family: var(--mono);
    font-size: var(--t-label);
    font-stretch: 87.5%;
    letter-spacing: 0.06em;
    color: var(--ink-3);
    line-height: 2.2;
  }
  dt::before {
    content: "Q.";
  }
  dd {
    margin: 10px 0 0;
    color: var(--ink-2);
    position: relative;
  }
  dd::before {
    content: "A.";
    top: 0;
  }
  b {
    font-weight: 600;
    color: var(--ink);
  }
  @media (max-width: 960px) {
    .press-head,
    .qa {
      grid-column: 1 / -1;
      position: static;
    }
  }
</style>
