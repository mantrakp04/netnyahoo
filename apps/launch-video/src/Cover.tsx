import { Launch } from "./Launch";
import { B } from "./timeline";

// The cover: round 3 just started, "Only 1% find him." over 503 suspects.
export const Cover: React.FC = () => <Launch at={B.r3.from + 8} />;
