--- a/components/update_client/update_checker.cc
+++ b/components/update_client/update_checker.cc
@@ -106,7 +106,9 @@
     scoped_refptr<UpdateContext> context,
     const base::flat_map<std::string, std::string>& additional_attributes,
     UpdateCheckCallback update_check_callback) {
-  return;
+  // ungoogled-chromium's block-requests.patch returned here, which left every
+  // update check pending forever: Web Store extensions never updated. The
+  // component updater's hosts stay blocked by domain substitution.
   DCHECK_CALLED_ON_VALID_SEQUENCE(sequence_checker_);
 
   update_check_callback_ = std::move(update_check_callback);
--- a/chrome/browser/extensions/updater/chrome_update_client_config.cc
+++ b/chrome/browser/extensions/updater/chrome_update_client_config.cc
@@ -224,7 +224,9 @@
   if (url_override_.has_value()) {
     return {*url_override_};
   }
-  return impl_.UpdateUrl();
+  // Domain substitution cuts off the component updater's update.googleapis.com;
+  // the Web Store's clients2.google.com (kept) answers the same protocol.
+  return {GURL("https://clients2.google.com/service/update2/json")};
 }
 
 std::vector<GURL> ChromeUpdateClientConfig::PingUrl() const {
